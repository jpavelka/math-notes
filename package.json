{
  "name": "math-notes",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "book"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=book",
    "build": "npm run build --workspace=book",
    "preview": "npm run preview --workspace=book",
    "pdf": "npm run pdf --workspace=book"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
